"**********************************************************************
Bundle 'a.vim'
if g:plugin_exist("a.vim")
	"echom "a.vim plugin"
else
	if g:load_vimrc_warn 
		echo "no a.vim"
	endif
endif
"**********************************************************************
