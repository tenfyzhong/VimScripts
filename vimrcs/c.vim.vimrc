"**********************************************************************
Bundle 'c.vim'
if g:plugin_exist("c.vim")
	" echom "c.vim plugin"
else
	if g:load_vimrc_warn 
		echo "no c.vim"
	endif
endif
"**********************************************************************
