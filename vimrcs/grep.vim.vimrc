"**********************************************************************
Bundle 'vim-scripts/grep.vim'
if g:plugin_exist("grep.vim")
	" echom "grep.vim plugin"
else
	if g:load_vimrc_warn 
		echo "no grep.vim"
	endif
endif
"**********************************************************************
